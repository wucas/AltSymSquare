#
# AltSymSquare: Computing the alternating and symmetric square representations of classical groups
#
# Implementations
#
InstallGlobalFunction( AlternatingSquare,
function(X)
	if not IsMatrixObj(X) then 
        ErrorNoReturn("X must be a MatrixObject");
    fi;

    

end );